# Claude

This is Slide. Start with `HANDOFF.md`, then pick the first unchecked box in `TASKS.md`.

Constraints: TypeScript + Vite + MapLibre. Smooth route is the default. No ads. Ghosts stay client-side until presence is designed.

If you add files, update the layout section in `HANDOFF.md`.
