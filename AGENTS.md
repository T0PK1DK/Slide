# Agents

You are helping finish **Slide** (https://github.com/T0PK1DK/Slide).

Read `HANDOFF.md` and `TASKS.md` before editing.

- Product: smoothest driving route + posted speeds + precise timing + 3D HUD + garage + Forza-style ghosts.
- Stack: Vite, TypeScript, MapLibre, public Valhalla + Photon + OpenFreeMap.
- Do not add a framework (React/Vue) unless a task requires it and you justify it.
- Do not replace OSM routing with Google Directions.
- Prefer extending `src/lib/*` over growing `main.ts` past reason.
- Match existing HUD CSS variables (`--glow`, `--mint`, glass panels).
- After a feature, `npm run build` must pass.

Owner will also use Grok in this repo. Leave notes in `HANDOFF.md` under **Session log** if you change a contract.
