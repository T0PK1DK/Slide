{
  "name": "slide",
  "version": "0.1.0",
  "private": true,
  "description": "Slide — smoothest-route driving navigation with posted speeds and precise timing",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.117.1",
    "maplibre-gl": "^5.6.1"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.1.5",
    "vitest": "^3.2.7"
  }
}
