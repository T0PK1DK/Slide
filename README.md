# Slide

Driver-first navigation that prefers the **smoothest** line.

Now with a pitched 3D map, Tesla/Uber-style HUD, a Garage for paint and camera, and Forza-style ghost cars on your route.

```bash
npm install
npm run dev
```

Open localhost, set From / To, hit **Drop the line**, then **Tune** to customize.
